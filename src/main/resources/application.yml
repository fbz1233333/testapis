spring:
  redis:
    timeout: 5000
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 300MB
mybatis:
  mapper-locations: classpath:mapper/*.xml
server:
  port: 9200



media:
  state:
    draft: state_draft
    complete: state_complete
  image:
    default: default.png
